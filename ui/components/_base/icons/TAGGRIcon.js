// DscvrIcon.jsx

import React from 'react';

function TAGGRIcon(props) {
  const { style, className } = props;

  return (
    <img src="/icons/TAGGR.svg"
      style={style} 
      className={className} 
      alt="TAGGR Icon"
    />
  );
}

export default TAGGRIcon;
